{
  "name": "memojigame-tile-match-bonus",
  "version": "1.0.0",
  "description": "A memory game where you have to find pairs of emojis",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run build && node src/index.js"
  },
  "author": "Paula Santamaría",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/readline-sync": "^1.4.8",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "readline-sync": "^1.4.10"
  }
}
